export default function LastSeenStatus({ uid }) {
  // Placeholder only – later fetch "lastSeen" timestamp
  return <p className="text-xs text-gray-500 dark:text-gray-400">Last seen recently</p>;
}
